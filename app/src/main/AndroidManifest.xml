<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fugg">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activity.Add_student"></activity>
        <activity android:name=".Activity.Add_supervisor" />
        <activity android:name=".Activity.Supervisor" />
        <activity android:name=".Activity.Create_Group" />
        <activity
            android:name=".Activity.Home_Fragment"
            android:label="@string/title_activity_test" />
        <activity android:name=".Activity.Request" />
        <activity android:name=".Activity.GroupsDetails" />
        <activity android:name=".Activity.MainActivity" />
        <activity android:name=".Activity.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>